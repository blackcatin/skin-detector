# ==============================================================================
# CONFIGURATION ENVIRONMENTS - SYIFAI BACKEND
# ==============================================================================

# 1. FASTAPI SERVER CONFIGURATION
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000

# 2. HUGGING FACE CREDENTIALS (ROOT CAUSE FIX)
# Pastikan token ini bertipe WRITE atau Fine-Grained Permissions
HF_TOKEN=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
HF_REPO_ID=blackcatin/resnet18-syifai

# 3. DATABASE POSTGRESQL CONNECTION (Untuk Logging & Fetching Data Retraining)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=123456
DB_NAME=syifai_db

# 4. MLFLOW TRACKING CONFIGURATION (Jika Terintegrasi untuk Registry)
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_EXPERIMENT_NAME=resnet18-retraining-pipeline